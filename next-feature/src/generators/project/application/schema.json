{
  "$schema": "https://json-schema.org/schema",
  "$id": "Application",
  "title": "Next.js Application Generator",
  "description": "Create a complete Next.js application with server-side rendering, API routes, TypeScript setup, and optional authentication and HTTP client configuration.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the application (e.g., web, api, dashboard). Creates apps/[name]/ with full Next.js application structure.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for this application?"
    },
    "directory": {
      "type": "string",
      "description": "Override the directory where the application will be created (default: apps/ creates apps/[name]/)",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "alias": "d"
    },
    "useAxios": {
      "type": "boolean",
      "description": "Setup Axios HTTP client with interceptors in lib/axios/ for API requests",
      "default": false
    },
    "useAuth": {
      "type": "boolean",
      "description": "Setup NextAuth.js authentication: adds the route handler and SessionProvider to this app, and creates a sibling '@feature/auth' library (via next-feature:feature --type=auth) if one doesn't already exist",
      "default": false
    },
    "orgName": {
      "type": "string",
      "description": "Organization name for scoped import paths (e.g., @myorg creates @myorg/[name] path alias)",
      "alias": "org"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip prettier code formatting",
      "default": false
    }
  },
  "required": ["name"]
}
