{
  "$schema": "https://json-schema.org/schema",
  "$id": "Feature",
  "title": "Feature Library Generator",
  "description": "Create a complete feature library with Next.js, TypeScript setup, optional authentication and HTTP client configuration. Creates apps/[name]/ with full project structure and all necessary configurations.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the feature project (e.g., users, products, dashboard). Creates directory apps/[name]/ with all project files and configuration.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for this feature?",
      "default": "base"
    },
    "directory": {
      "type": "string",
      "description": "Override the directory where the feature project will be created (e.g., libs/ instead of apps/)",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "alias": "d"
    },
    "orgName": {
      "type": "string",
      "description": "Organization name for scoped import paths (e.g., @myorg creates @myorg/[name] path alias)",
      "alias": "org"
    },
    "type": {
      "type": "string",
      "description": "Type of feature library to create. 'base' includes base components and logging, 'logging' includes only logging config, 'client' creates a reusable API client library with error handling and utilities, 'auth' creates a NextAuth.js configuration with credential + OAuth providers, 'generic' is a plain feature library.",
      "enum": ["base", "logging", "client", "auth", "generic"],
      "default": "generic",
      "alias": "t"
    },
    "useAxios": {
      "type": "boolean",
      "description": "Setup Axios HTTP client with interceptors in lib/axios/ for API requests",
      "default": false
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip prettier code formatting (used internally for chained generators)",
      "default": false
    }
  },
  "required": ["name"]
}
