
interface Props {
  params: Promise<{}>;
  searchParams: Promise<{}>;
}


async function <%= names.className %>Page(props: Props) {
  const params = await props.params;

  return (
    <>
      <%= names.className %> Page
    </>
  );
}

export default <%= names.className %>Page;
