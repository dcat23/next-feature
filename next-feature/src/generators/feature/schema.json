{
  "$schema": "https://json-schema.org/schema",
  "$id": "Feature",
  "title": "Feature Generator",
  "description": "Initializes the features plugin to use with lib generators",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the project features will be added to.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "directory": {
      "type": "string",
      "description": "Where to create this project",
      "default": "."
    },
    "srcPath": {
      "type": "string",
      "description": "The directory where features files will generated to",
      "default": "src"
    },
    "useAxios": {
      "type": "boolean",
      "description": "Initialize axios package",
      "default": false
    },
    "useAuth": {
      "type": "boolean",
      "description": "Initialize auth package",
      "default": false
    },
    "useDb": {
      "type": "boolean",
      "description": "Initialize database files",
      "default": false
    },
    "useAll": {
      "type": "boolean",
      "description": "Will initialize all packages (auth, axios, database)",
      "default": false
    }
  },
  "required": ["name"]
}
