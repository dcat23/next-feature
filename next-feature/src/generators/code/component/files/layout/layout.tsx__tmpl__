import { ReactNode } from 'react';

interface Props {
  children: ReactNode
}

async function <%= names.className %>Layout(props: Props) {
  return (
    <>
      {props.children}
    </>
  );
}

export default <%= names.className %>Layout;

