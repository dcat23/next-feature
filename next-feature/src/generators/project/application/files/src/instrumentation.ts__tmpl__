<% if (!skipLogging) { %>
import { registerPino } from '@next-feature/logging/server';

export function register() {
  registerPino();
}
<% } else { %>
export function register() {
  // Logging was skipped for this app (skipLogging: true).
}
<% } %>