{
  "$schema": "https://json-schema.org/schema",
  "$id": "Axios",
  "title": "",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the feature project",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What project should this packaged be added to?",
      "default": "base",
      "alias": "p"
    },
    "appProjectName": {
      "type": "string",
      "description": "App project to add env files too",
      "alias": "a"
    }
  },
  "required": ["projectName"]
}
