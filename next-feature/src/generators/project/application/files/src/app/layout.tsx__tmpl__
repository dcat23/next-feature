import './global.css';
import Providers from './providers';
import { Layout } from '../components/layout';
import { ReactNode } from 'react';

export const metadata = {
  title: '<%= name %>',
  description: 'Generated by @dcat23/next-feature',
}

export default function RootLayout({
  children,
}: {
  children: ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <Providers>
          <Layout>
            {children}
          </Layout>
        </Providers>
      </body>
    </html>
  )
}
