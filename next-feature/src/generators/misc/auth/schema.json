{
  "$schema": "https://json-schema.org/schema",
  "$id": "Auth",
  "title": "",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name for the features generator plugin",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What project should this package be added to?",
      "alias": "p"
    }
  },
  "required": ["projectName"]
}
