{
  "$schema": "https://json-schema.org/schema",
  "$id": "Preset",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the project features will be added to.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "directory": {
      "type": "string",
      "description": "Where to create this project",
      "alias": "d"
    },
    "skipFeature": {
      "type": "boolean",
      "description": "Will skip creating the feature plugin",
      "default": false
    }
  },
  "required": ["name"]
}
